[package]
name = "nucleus_macros"
version = "1.0.0"
edition = "2024"
publish = ["gitea"]
license = "MIT"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full", "visit-mut"] }
quote = "1.0"
proc-macro2 = "1.0"
async-trait = "0.1.89"

[features]
default = []
static_registry = []
smallbox = []
